<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.8.0/css/bulma.min.css">
  </head>

  <body>
    <div class="mb-4">
      <%= link_to "Back to Homepage", root_path, class: "button is-link" %>
    </div>

    <section class="section">
      <div class="container">
        <h2 class="title is-2">Edit <%= resource_name.to_s.humanize %></h2>

        <h3 class="title is-4">Past Orders</h3>
        <%= link_to "View Past Orders", customer_orders_path(current_customer), class: "button is-primary" %>

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <!-- /br = line break-->
          <div class="field">
            <%= f.label :first_name %><br />
            <%= f.text_field :first_name, autofocus: true, class: "input" %>
          </div>

          <div class="field">
            <%= f.label :last_name %><br />
            <%= f.text_field :last_name, class: "input" %>
          </div>

          <div class="field">
            <%= f.label :address %><br />
            <%= f.text_field :address, class: "input" %>
          </div>

          <div class="field">
            <%= f.label :province %><br />
            <%= f.select :province, options_for_select([
              ["Alberta", "Alberta"],
              ["British Columbia", "British Columbia"],
              ["Manitoba", "Manitoba"],
              ["New Brunswick", "New Brunswick"],
              ["Newfoundland and Labrador", "Newfoundland and Labrador"],
              ["Nova Scotia", "Nova Scotia"],
              ["Ontario", "Ontario"],
              ["Prince Edward Island", "Prince Edward Island"],
              ["Quebec", "Quebec"],
              ["Saskatchewan", "Saskatchewan"]
            ], @customer.province), { include_blank: "Select Province" }, class: "input" %>
          </div>

          <div class="field">
            <%= f.label :phone_number %><br />
            <%= f.telephone_field :phone_number, class: "input" %>
          </div>

          <!-- Devise default fields -->
          <div class="field">
            <%= f.label :email %><br />
            <%= f.email_field :email, autocomplete: "email", class: "input" %>
          </div>

          <div class="field">
            <%= f.label :password %> <i>(leave blank if you don't want to change it)</i><br />
            <%= f.password_field :password, autocomplete: "new-password", class: "input" %>
          </div>

          <div class="field">
            <%= f.label :password_confirmation %><br />
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "input" %>
          </div>

          <div class="field">
            <%= f.label :current_password %> <i>(we need your current password to confirm your changes)</i><br />
            <%= f.password_field :current_password, autocomplete: "current-password", class: "input" %>
          </div>

          <div class="actions">
            <%= f.submit "Update", class: "button is-link" %>
          </div>
        <% end %>

        <!--<div><h3 class="title is-4">Cancel my account</h3>


          Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), method: :delete, data: { confirm: "Are you sure?" }, class: "button is-danger" %>
        </div>-->
      </div>
    </section>
  </body>
</html>
